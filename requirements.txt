# requirements.txt
# Python dependencies for TearScope build system

# Build and compilation tools
cmake>=3.20
ninja>=1.10.0

# Python packages with pre-built wheels (avoiding compilation issues)
pyzstd>=0.15.0; python_version<'3.14'
bcj-cffi>=0.5.0

# Additional utilities
setuptools>=65.0
wheel>=0.38.0
